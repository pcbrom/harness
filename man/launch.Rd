% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launch.R
\name{launch}
\alias{launch}
\title{Launch a curated coding session}
\usage{
launch(
  adapter = "claude",
  role,
  project_dir = getwd(),
  scaffold = TRUE,
  dry_run = FALSE,
  config_home = NULL,
  skills_path = NULL,
  binary = NULL
)
}
\arguments{
\item{adapter}{The coder to launch. See \code{\link[=adapters]{adapters()}}.}

\item{role}{The professional role. See \code{\link[=available_roles]{available_roles()}}.}

\item{project_dir}{The project root. Defaults to the working directory.}

\item{scaffold}{When \code{TRUE} (the default), creates the role's folder layout.}

\item{dry_run}{When \code{TRUE}, configures everything but does not open a
terminal. Useful for inspection and testing.}

\item{config_home}{Override the adapter configuration home (mainly for
testing).}

\item{skills_path}{Override the community-skills checkout path.}

\item{binary}{Override the discovered coder binary path.}
}
\value{
An object of class \code{harness_launch}, invisibly.
}
\description{
Loads the harness for \code{role}, configures the chosen \code{adapter} for the
project, scaffolds the role's folder layout, and opens the coder in a
terminal tab. Inside RStudio the terminal is created with
\code{rstudioapi::terminalCreate}; otherwise an external terminal emulator is
used, and when none is available the launch command is reported for the user
to run.
}
\details{
The package never runs an agent loop and never executes code produced by the
agent. Generated scripts are run manually by the user.
}
\examples{
\dontrun{
launch("claude", role = "data-scientist")
}
}
