% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conventions.R
\name{scaffold_layout}
\alias{scaffold_layout}
\title{Scaffold the folder layout of a role}
\usage{
scaffold_layout(role_name, project_dir = getwd(), create = FALSE)
}
\arguments{
\item{role_name}{A role name, as returned by \code{\link[=available_roles]{available_roles()}}.}

\item{project_dir}{The project root under which to create the layout.}

\item{create}{When \code{FALSE} (the default), the function reports the directories
that would be created without touching the filesystem. Set to \code{TRUE} to
create them.}
}
\value{
A data frame with one row per layout entry, invisibly.
}
\description{
Creates the directories declared in the harness \code{layout} under
\code{project_dir}. Existing directories are left untouched. The function never
writes code, runs a script, or removes anything; it only ensures the audit
folders exist.
}
\examples{
tmp <- tempfile("proj")
dir.create(tmp)
scaffold_layout("data-scientist", tmp, create = TRUE)
}
